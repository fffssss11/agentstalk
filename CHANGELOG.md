# Changelog

## Unreleased

- 新增 Windows 便携包 `agentstalk-版本-windows-x64.zip`：自带 python.org 官方嵌入式 Python 3.13（版本与 SHA-256 固定，文件原样收录），解压后双击 `启动看板.cmd` 即可使用，不需要安装 Python；agent 的 skill 命令也使用包内 Python。发布工作流会在真实 Windows 机器上重建并启动便携包，核对字节与 Linux 构建一致后才上传。
- 第一次启动时询问是否在桌面创建 agentstalk 图标：已有打开本目录的图标时不询问，图标打开的是另一个副本时询问是否改为本目录；从 0.2.0 升级的目录也会被问一次。macOS/Linux 首次运行 `sh scripts/launch.sh` 时同样询问。
- 升级：便携包会一并更新内置 Python，源码包升级保留已有的内置 Python，并拒绝用正在被替换的内置 Python 运行升级。
- `hub.py doctor` 在隔离模式的 Python 下也能找到 `scripts/`；发布说明提到便携包的下载与用法。
- 首次运行提示中的 skill 安装命令写出实际使用的 Python，便携包用户电脑上没有 `python` 命令也能照着运行。
- README 与英文版重写：功能一览、三分钟上手、工作原理图、支持的客户端、系统要求与常见问题；界面截图改为隔离演示数据生成的协作现场、流程、深色主题与窄屏画面。
- 源码包的 `.sha256` 校验文件在所有平台都使用 LF 换行，Windows 本机构建与 CI 构建的 `SHA256SUMS.txt` 保持一致；已发布的 0.2.0 附件由 CI 构建，不受影响。

## 0.2.0

首个正式版：重构网页工作台，加入成员实时画面、项目与会话、事件要点与主题，新增桌面图标与首次运行检查，以及维护、升级和发布工具。

- 拒绝会破坏后续读取的事件字段类型，同请求编号异内容会报错；心跳、绑定和未读游标采用原子替换。
- 成员 CLI 读取必须指定身份，人工终端使用 `status --human`；监听续接保留完整数据目录与配置参数。
- 浏览器存储故障时保留页面内草稿与发送能力，补充持久化限制提示；修复输入法组合期间快捷键误发送和窄屏导航焦点控制。
- 加速长历史的文本用量估算，保持原有估算公式与 Unicode 计数规则。
- 发布检查拒绝私有路径大小写变体、跨平台重名及现代 GitHub PAT；归档审计逐个扫描重名成员，安装器拒绝损坏的归属清单。
- 修复 Windows 启动器的相对数据/配置路径解析，停止服务时增加配置身份核对；浏览器测试核对隔离服务 PID。
- 本轮实测与限制见 [维护报告](docs/maintenance-2026-09-18.md)。
- 侧栏、全员对话和任务工作树增加放射形工作标志，依据进行中任务与近期读取显示；暂停、离线、交付或受阻时停止。
- 工作树增加最近派发与转交记录，显示真实发送者、当时接收者、任务内容和时间，新同步派发播放短暂传递动效。
- 保留系统减少动态效果偏好，增加隔离浏览器验证。
- 重构网页工作台：左侧会话栏、顶部视图切换（现场 / 流程 / 记录）和可调宽度的右侧面板（对话 / 任务 / 成员），界面拆分为多个本地脚本，窄屏改为底部标签栏。
- 新增「现场」：通过浏览器窗口共享实时查看各成员窗口，提供宫格、聚焦、单屏布局、自动跟随、发言字幕、静止与不可见检测、悬浮窗，以及确认后才附加的截图。画面只在本机显示，不录制、不上传，也不能控制窗口；远程控制暂不提供。
- 会话管理改为项目与单独会话：项目预设新会话的模式、主导、成员与共享上下文；会话可重命名（仅面板显示）、置顶、移动、归档和搜索。整理信息存入 `.library.json`，不进入 agent 事件日志。
- 对话流实时显示每次事件的一行要点，可逐条展开或整体切换为详细；新增「需要处理」汇总和后台桌面通知。
- 新增淡绿（默认）、浅色、深色、墨绿、暖光、高对比度主题，可跟随系统明暗、自定义配色并导入导出，支持界面字号。
- 新增维护脚本 `scripts/maintain.py`（状态、发布关卡、版本号、构建、干净副本验证、同步公开克隆）和原地升级脚本 `scripts/upgrade.py`（预览、备份、回滚，不改动私人数据）；推送 `v*` 标签后 GitHub 工作流在测试通过后生成草稿 Release。流程见 [维护与发布流程](docs/maintenance.md)。
- CI 增加发布清单与版本页一致性检查，同一分支的新提交会取消旧运行。
- GitHub 仓库更名为 `agentstalk`，源码包改名为 `agentstalk-版本号.zip`；skill 名称、页面存储与健康接口标识保持不变，已有安装不受影响。
- 新增桌面图标：Windows 双击 `创建桌面图标.cmd`，macOS/Linux 运行 `sh scripts/desktop.sh`。双击图标会检查 Python（Windows 可在确认后用 winget 安装）、首次运行询问是否为已检测到的客户端安装 skill，然后启动控制面板；图标与面板品牌标志一致，由 `scripts/make_icons.py` 可复现生成。
- skill 安装器新增 `--status` 与 `--setup`；没有归属清单、但由其他目录安装的旧版 skill 需要 `--replace-project` 才会改写，避免误把正在使用的客户端指向另一份副本。
- Windows Python 查找增加 python.org 标准安装位置，Python 路径含中文等非英文字符时也能正确识别；桌面图标以 Unicode 方式写入，英文版 Windows 上的中文目录同样可用。启动错误改为中文提示，启动失败时附带环境检查结果。

## 0.1.0-rc.3

公开开源预发布版。

- 应用 MIT 许可证，完善项目元数据、贡献与安全入口。
- 将项目介绍压缩至 6 页，补充 54 秒 1080p/60fps 宣传视频及可复现动画源。
- 宣传画面使用隔离截图，配乐由脚本合成，不含真实聊天或模型调用记录。
- CI 发布检查要求许可证，新增许可证一致性验证。
- 运行程序沿用 rc.2，发布包与本机协作数据保持分离。

## 0.1.0-rc.2

GitHub 发布准备与使用材料。

- 新增首次协作示例，连接实例配置、skill 接入、交付和验收。
- 增加源码、Git 追踪清单、提交邮箱及 PPTX/PDF 隐私扫描与回归测试。
- 补充功能建议模板、三轮发布审查清单及项目介绍文案。
- 增加 12 页可编辑项目介绍 PPT、PDF 阅读版及明确标注的隔离演示截图。
- 不自动公开个人数据，不把演示结果计入真实测试或模型调用。
- 修复英文 Windows 控制台输出中文发布文件名及安装目录时报编码错误的问题，并增加旧编码环境回归。
- Windows 优先验证明确指定的 Python，避免无关启动器探测；新服务启动使用计时边界与 PID 核对，失败时仅清理本次子进程。

## 0.1.0-rc.1

首次可分发源码候选版，延续现有 Agents Talk 本地实现。

- 同客户端独立实例、主导/执行/验收角色、共享对话与实时工作树。
- 人工干预、参与名单、摘要与增量上下文、媒体能力门控和真实 usage 去重。
- Python 标准库前台启动、配置回退、环境诊断和轻量健康接口。
- 可选择客户端的 skill 安装、预览、个人路径隔离和保守卸载。
- Windows 启动/停止身份核对，跨平台测试入口与 CI 配置。
- 精确源码发布清单、个人数据排除、文件和归档 SHA-256。

许可证及公开发布状态见 README；真实验证范围见 docs/release.md。
